# 企业客户工单关闭决策

`POST /api/enterprise/customer-service/case-resolution-release` 在工单关闭前检查客户身份、服务权益、严重度、解决方案验证、SLA 补偿、客户验收、职责分离与审计证据。

- `RESOLVE`：服务结果与控制证据完整，可以关闭工单。
- `ESCALATE`：不存在硬性阻断，但仍需补充根因、客户确认、违约补偿或知识沉淀。
- `BLOCKED`：身份、权益、严重度、方案验证、职责分离或审计控制失败。

该决策点可供客服工作台、知识库、赔付和消息通知适配器调用。
